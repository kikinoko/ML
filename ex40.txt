本レポートでは、最急降下法（Gradient Descent）を用いて関数 y=x^2の最小値を求めるアルゴリズム。
最急降下法は、勾配（微分）を用いて関数の極値を探索する手法で初期値を設定し、学習率に基づいて勾配方向に進むことで目的関数の最小値に収束させている。




x の初期値で、最適化を始めるスタート地点となります。
learning_rate (float): 学習率と呼ばれ、勾配に基づいて更新される量を調整し。
この値が大きいと、ステップが大きくなり、小さいとステップが小さくなる。
max_iters (int): 最急降下法の最大イテレーション（反復）回数です。